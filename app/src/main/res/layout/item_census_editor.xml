<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Image -->
        <ImageView
            android:id="@+id/imgCensus"
            android:layout_width="match_parent"
            android:layout_height="110dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_placeholder_image" />

        <!-- Nom -->
        <TextView
            android:id="@+id/tvName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="6dp"
            android:paddingBottom="2dp"
            android:maxLines="2"
            android:ellipsize="end"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:fontFamily="@font/kg_blank_space_solid" />

        <!-- Boutons action : crayon + croix -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:paddingEnd="4dp"
            android:paddingBottom="4dp">

            <!--
                On utilise des MaterialButton avec icône pour garantir la visibilité
                même si ic_edit / ic_close ne sont pas dans le projet.
                Remplace iconSrc par tes propres drawables si besoin.
            -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnEdit"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton.Icon"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginEnd="4dp"
                android:text="✏"
                android:textSize="16sp"
                android:textColor="?attr/colorPrimary"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:minWidth="0dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                app:strokeColor="?attr/colorPrimary"
                app:strokeWidth="1dp"
                android:contentDescription="Modifier" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDeleteNode"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton.Icon"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="✕"
                android:textSize="16sp"
                android:textColor="@color/red"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:minWidth="0dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                app:strokeColor="@color/red"
                app:strokeWidth="1dp"
                android:contentDescription="Supprimer" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>