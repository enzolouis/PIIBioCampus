<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_rounded_white"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Titre -->
        <TextView
            android:id="@+id/tvEditorTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ajouter un élément"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:layout_marginBottom="16dp"
            android:fontFamily="@font/kg_blank_space_solid" />

        <!-- Image preview -->
        <ImageView
            android:id="@+id/ivNodeImagePreview"
            android:layout_width="match_parent"
            android:layout_height="160dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_placeholder_image"
            android:background="#F0F0F0"
            android:layout_marginBottom="8dp" />

        <!-- Bouton choisir image -->
        <Button
            android:id="@+id/btnPickImage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Choisir une image"
            android:textAllCaps="false"
            android:layout_marginBottom="16dp"
            style="@style/ButtonPrimaryOutlined" />

        <!-- Champ Nom — même style que la page de connexion -->
        <EditText
            android:id="@+id/etNodeName"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/fields_round_border"
            android:hint="Nom *"
            android:inputType="textCapSentences"
            android:maxLines="1"
            android:paddingStart="10dp"
            android:textColorHint="@color/grey"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:fontFamily="@font/gentium_plus_regular"
            android:layout_marginBottom="12dp" />

        <!-- Champ Description — même style que la page de connexion -->
        <EditText
            android:id="@+id/etNodeDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/fields_round_border"
            android:hint="Description (une ligne par paragraphe)"
            android:inputType="textMultiLine|textCapSentences"
            android:minLines="4"
            android:gravity="top|start"
            android:paddingStart="10dp"
            android:paddingTop="10dp"
            android:paddingEnd="10dp"
            android:paddingBottom="10dp"
            android:textColorHint="@color/grey"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:fontFamily="@font/gentium_plus_regular"
            android:layout_marginBottom="20dp" />

        <!-- ProgressBar upload -->
        <ProgressBar
            android:id="@+id/progressBarEditor"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="8dp"
            android:visibility="gone" />

        <!-- Boutons Annuler / Enregistrer -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnCancelNode"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="6dp"
                android:text="Annuler"
                android:textAllCaps="false"
                style="@style/ButtonDanger" />

            <Button
                android:id="@+id/btnSaveNode"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="6dp"
                android:text="Enregistrer"
                android:textAllCaps="false"
                style="@style/ButtonSuccess" />

        </LinearLayout>

    </LinearLayout>
</ScrollView>